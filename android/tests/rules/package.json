{
  "name": "apptesting-rules-tests",
  "private": true,
  "description": "Firestore security rule tests. Kept out of functions/ so nothing here is ever deployed.",
  "scripts": {
    "test": "node --test \"*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
